import type { RequestParam, ParsedPath, Signature } from "./types";
type RequestParamInternal = RequestParam & {
    signQuery?: boolean;
};
export default class RequestSigner {
    secret: string;
    request: RequestParamInternal;
    parsedPath: ParsedPath;
    datetime: string;
    constructor(request: RequestParam, secret?: string, headersToInclude?: string[]);
    private prepareRequest;
    sign(): Signature;
    signQuery(): Signature;
    private getDateTime;
    private signature;
    private stringToSign;
    private canonicalString;
    private canonicalHeaders;
    private signedHeaders;
    private parsePath;
    private formatPath;
}
export {};
